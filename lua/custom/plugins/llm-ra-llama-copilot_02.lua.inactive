local hostname = vim.loop.os_gethostname()
-- moriarty CPU spins out of control with tabnine running

if string.find(hostname, "terminusZZZZ") then
	return {
		"Faywyn/llama-copilot.nvim",
		requires = "nvim-lua/plenary.nvim",
		opts = {
			host = "localhost",
			port = "11434",
			model = "starcoder2",
			max_completion_size = 15, -- use -1 for limitless
			debug = false,
		},
	}
else
	return {}
end
